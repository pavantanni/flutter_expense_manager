name: expense_manager
description: A Flutter expense manager application.

version: 1.0.0+1

environment:
  sdk: ">=2.17.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  # State management (null-safe)
  provider: ^6.1.5+1

  # Local storage (null-safe)
  shared_preferences: ^2.3.3

  # iOS-style icons (null-safe)
  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
